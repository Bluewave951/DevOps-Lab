name: DevOps CI

on:
  push:
    branches: [ main ]

jobs:
 build:
  runs-on: ubuntu-latest

  steps:

  - name: Checkout
    uses: actions/checkout@v4

  - name: Build Docker
    run: docker build -t devops-lab .

  - name: Test container
    run: docker run -d -p 3000:3000 devops-lab